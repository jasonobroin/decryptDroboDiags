// decryptDiags_test
package main

import "testing"

func TestNew(t *testing.T) {
	t.Error("Need to write some tests")
}

// Encrypt some known text, and then decrypt and validate its what we encrypted
func TestDecrypt(t *testing.T) {
	t.Error("Need to write decrypt file test")
}
